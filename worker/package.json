{
  "name": "loeschmeier-abo-worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "node deploy.mjs",
    "deploy:roh": "wrangler deploy",
    "test": "node --test test/*.test.js"
  },
  "type": "module",
  "devDependencies": {
    "wrangler": "^3"
  }
}
